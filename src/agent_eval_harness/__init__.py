"""agent-eval-harness: evaluation harness for LLM agents.

Scaffold only — the core eval API (datasets / runners / metrics / judges) is
designed in the build phase (see ../progress.md). Kept intentionally empty so
the architecture is decided deliberately, not pre-generated.
"""

__version__ = "0.0.1"
